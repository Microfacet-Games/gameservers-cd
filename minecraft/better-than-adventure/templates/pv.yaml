apiVersion: v1
kind: PersistentVolume
metadata:
  name: better-than-adventure-pv
  namespace: gameservers
  labels:
    type: local
spec:
  capacity:
    storage: 10Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-path
