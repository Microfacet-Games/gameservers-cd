apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: better-than-adventure-pvc
  namespace: gameservers
spec:
  storageClassName: local-path
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
